<%- model_class = Trip -%>
  <div class="col-sm-10 col-sm-offset-1">
    <div class="row ">
      <div class="page-header col-xs-9">
        <span class="main-heading"><%=t '.title', :default => Trip %></span>
      </div>
      <div class="new-project-btn-div col-xs-3">
        <%= link_to new_trip_path,
                :class => 'btn btn-default new-project' do%>
          <i class="fa fa-folder-o "></i> New Trip
        <%- end -%>
      </div>
    </div>
    <div class="row">
      <div class="col-sm-9">
       <!--  <table class="table table-striped">
          <thead>
            <tr>
              <th>ID</th>
              <th><%= model_class.human_attribute_name(:name) %></th>
              <th><%= model_class.human_attribute_name(:mode) %></th>
              <th><%= model_class.human_attribute_name(:description) %></th>
              <th><%= model_class.human_attribute_name(:created_at) %></th>
              <th><%=t '.actions', :default => t("helpers.actions") %></th>
            </tr>
          </thead>
          <tbody> -->
            <% @trips.each do |trip| %>
              <div class="row grey-border trip-list-row">
                <div class="col-xs-1 col-xs-offset-1 trip-padding add-trip-margin-bottom">
                  <i class="fa fa-folder-o fa-2x"></i> 
                </div>
                <div class="col-xs-3 add-trip-margin-bottom">
                  <div class="trip-box-name "><%= trip.name %></div>
                  <div><%=l trip.created_at, format: :long %></div>
                </div>
                <div class="col-xs-1 col-xs-offset-4 trip-padding add-trip-margin-bottom">
                   <%= link_to trip_path(:id => trip.name), :method => :get, :class => '', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
                    <i class="fa fa-eye fa-2x"></i>
                  <%- end -%>
                </div>
                <div class="col-xs-2 trip-padding add-trip-margin-bottom">
                   <%= link_to trip_path(:id => trip.name), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => '', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
                       <i class="fa fa-trash-o fa-2x"></i>
                    <%- end -%>
                </div>
                <!-- <div class="col-sm-8 ">
                    <%= trip.id %>
                    
                    <%= trip.mode %>
                    <%= trip.description %>
                    <%=l trip.created_at, format: :long %>
                </div> -->
              </div>
              <!-- <tr>
                <td><%= trip.id %></td>
                <td><%= trip.name %></td>
                <td><%= trip.mode %></td>
                <td><%= trip.description %></td>
                <td><%=l trip.created_at, format: :long %></td>
                <td>
                  <%= link_to trip_path(trip), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
                    <%= glyph 'info-sign' %>
                    <%= glyph 'pencil' %>
                  <%- end -%>
                  <%= link_to trip_path(trip), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
                    <%= glyph 'remove' %>
                  <%- end -%>
                </td>
              </tr> -->
            <% end %>
         <!--  </tbody>
        </table> -->
      </div>
    </div>

    

  </div>
