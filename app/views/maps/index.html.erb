<%- model_class = Folder -%>
  <div class="col-sm-10 col-sm-offset-1">
    <div class="row ">
      <div class="page-header col-xs-9">
        <span class="main-heading"><%=t '.title', :default => Trip %>/<%= @trip.name %>/<%= @folder.name %></span>
      </div>
      <div class="new-project-btn-div col-xs-3">
        <%= link_to new_trip_folder_path(@trip),
                :class => 'btn btn-default new-project' do%>
          <i class="fa fa-folder-o "></i> New Folder
        <%- end -%>
      </div>
    </div>
    <div class="row">
      <div class="col-sm-3">
        <ul class="nav nav-pills nav-stacked custom-sidebar">
          <li><%= link_to 'Descriptions',
              trip_folder_path(:trip_id => @trip.name, :id => @folder.name) %>
          </li>
          <li class="sidebar-active"><%= link_to 'Maps',
              trip_folder_maps_path(:trip_id => @trip.name, :folder_id => @folder.name) %>
          </li>
          <li><a href="">Cost</a></li>
          <li><a href="#">Photos</a></li>
          <li><a href="#">Videos</a></li>
        </ul>
      </div>
      <div class="col-sm-9">
        <div class="row">
          <% @folder.maps.each do |map| %>
            <div class="col-xs-4">
              <div class="panel panel-default folder-display-panel" data-folder-path="<%=trip_folder_map_path(:trip_id => @trip.name,:folder_id => @folder.name, :id => map.name)%>">
                <div class="panel-body folders-display-body">
                  <div class="row">
                    <div class="col-xs-12">
                      <%= raw map.description %>
                    </div>
                    <div class="col-xs-12 description">
                        <%= map.updated_at %>
                    </div>
                  </div>
                </div>
                <div class="panel-footer"><h5><%= map.name.capitalize %></h5></div>
              </div>
            </div>
          <% end %>
            <div class="col-sm-4">
              <div class="panel panel-default ">
                <div class="panel-body">
                    New Folder
                    <%= link_to new_trip_folder_map_path(:trip_id => @trip.name, :folder_id => @folder.name), :method => :get, :class => '', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
                       <i class="fa fa-plus fa-2x"></i>
                    <%- end -%>
                </div>
                <div class="panel-footer">Panel footer</div>
              </div>
            </div>
        </div>
      </div>
    </div>   
  </div>
<script>
  $(function(){
    $(document).on('click','.folder-display-panel', function(){
      window.location.href = $(this).attr("data-folder-path");
    })
  })

</script>

<!-- <h1>Listing maps</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Mode</th>
      <th>Description</th>
      <th>Transport</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @maps.each do |map| %>
      <tr>
        <td><%= map.name %></td>
        <td><%= map.mode %></td>
        <td><%= map.description %></td>
        <td><%= map.transport %></td>
        <td><%= link_to 'Show', map %></td>
        <td><%= link_to 'Edit', edit_map_path(map) %></td>
        <td><%= link_to 'Destroy', map, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Map', new_map_path %> -->
